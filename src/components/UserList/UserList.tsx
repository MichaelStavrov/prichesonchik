// import { getUser } from '@/app/main';

const UserList = async () => {
  // const users = await getUser();

  return (
    <div>
      User list
      {/* <ul>
        {users.map((user) => (
          <li key={user.id} style={{ marginBottom: 12 }}>
            <div>{user.name}</div>
            <div>{user.phone}</div>
          </li>
        ))}
      </ul> */}
    </div>
  );
};

export default UserList;
